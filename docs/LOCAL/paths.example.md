# Example — copy to a **new** file in this folder with your own name (untracked / ignored)

The repository does **not** commit real PC paths or host secrets.

Example content for your private file:

```text
Clone root:     (your path)
Gradle:         ./gradlew build
JAR:            build/libs/flowinventory-x.y.z.jar
```

Everything in `docs/LOCAL/` is ignored **except** `*.example.md` and `.gitkeep`.
